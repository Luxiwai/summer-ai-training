"""
五子棋项目全部常量定义。
集中管理可消除魔法值和硬编码耦合，修改一处即可全局生效。
"""

# ============================================================
# 棋盘几何参数
# ============================================================
BOARD_SIZE = 10                     # 10×10 棋盘
CELL_SIZE = 50                      # 每个格子的像素大小
MARGIN_X = 50                       # 左侧留白（行标签 A-J 的空间）
MARGIN_Y = 50                       # 顶部留白（列标签 0-9 的空间）
BOARD_PIXEL = BOARD_SIZE * CELL_SIZE  # 500 px — 实际对弈区域

# ============================================================
# 窗口尺寸（由棋盘参数推导 —— 单一真相源）
# ============================================================
WINDOW_WIDTH = MARGIN_X * 2 + BOARD_PIXEL       # 600 px
WINDOW_HEIGHT = MARGIN_Y * 2 + BOARD_PIXEL + 80  # 680 px（底部 80px 状态栏）

# 文本定位辅助量
BOARD_BOTTOM = MARGIN_Y + BOARD_PIXEL           # 550 — 棋盘底边
STATUS_CENTER_Y = BOARD_BOTTOM + 40              # 590 — 状态文字居中
WINNER_TEXT_Y = BOARD_BOTTOM + 45               # 595 — 获胜信息
HINT_TEXT_Y = BOARD_BOTTOM + 75                  # 625 — 操作提示

# ============================================================
# 颜色定义
# ============================================================
COLOR_BG = (222, 184, 135)          # 木色背景
COLOR_GRID = (0, 0, 0)              # 网格线
COLOR_BLACK_PIECE = (20, 20, 20)    # 黑子填充
COLOR_WHITE_PIECE = (240, 240, 240) # 白子填充
COLOR_OUTLINE = (0, 0, 0)           # 棋子外描边（1px）
COLOR_LABEL = (60, 30, 0)           # 行列标签文字颜色

COLOR_STATUS_NORMAL = (0, 0, 0)     # 常规状态文字（如"黑方回合"）
COLOR_STATUS_ERROR = (200, 0, 0)    # 错误提示文字
COLOR_STATUS_WIN = (0, 140, 0)      # 获胜信息文字
COLOR_HINT = (120, 120, 120)        # 次要提示文字

# ============================================================
# 棋子 / 格子标记
# ============================================================
EMPTY = '-'     # 空格
BLACK = '*'     # 黑子（先手）
WHITE = 'o'     # 白子（后手）

# ============================================================
# 玩家编码
# ============================================================
PLAYER_BLACK = 1
PLAYER_WHITE = -1

# 玩家常量 → 显示名称映射
PLAYER_NAME = {
    PLAYER_BLACK: "黑方 (*)",
    PLAYER_WHITE: "白方 (o)",
}
